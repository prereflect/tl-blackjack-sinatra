<h4>
  Toby's cards
  <% if session[:player_turn] %>
    <span>
    <%= erb :_hit_stay_buttons %>
    </span>
    Total points: <%= calculate_total(session[:player_hand]) %>
  <% else %>
    <span>&cir;</span>Total points: <%= calculate_total(session[:player_hand]) %>
  <% end %>
</h4>

<% session[:player_hand].each do |card| %>
  <%= generate_pic_url(card) %>
<% end %>

